/**
 * Created by hot on 2016-6-15.
 */
(function (angular) {
	'use strict';

	angular.module('App.common').directive('contactsFooter',
		['$state', '$translate',
			function ($state, $translate) {
				return {
					restrict: 'AE',
					replace: true,
					scope: {
						switchAxes: '=',
						reselectDocument: '=',
						hasAxes: '='
					},
					templateUrl: globals.appBaseUrl + 'common/partials/contacts-footer.html',
					link: function (scope, element, attrs) {

						/*scope.hasReport = attrs.hasReport ? scope.$eval(attrs.hasReport) : false;
						scope.hasHome = attrs.hasHome ? scope.$eval(attrs.hasHome) : false;
						scope.goHome = function () {
							$state.go('report.documents');
						};
						scope.hasUserInfo = attrs.hasUserInfo ? scope.$eval(attrs.hasUserInfo) : false;
						scope.goToUserInfo = function () {
							app.headerTitle = $translate.instant('main.appName');
							$state.go('userInfo');
						};*/

					}
				};
			}]);
})(angular);